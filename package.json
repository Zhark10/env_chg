{
  "name": "env_config",
  "version": "1.0.0",
  "description": "change env conf",
  "main": "index.js",
  "scripts": {
    "env:dev": "cat dev.env > .env && node index.js",
    "env:prod": "cat prod.env > .env && node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}
